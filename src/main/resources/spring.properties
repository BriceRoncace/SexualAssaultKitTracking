application.uri = ${application.uri}

# In test mode, you can logon as an admin using admin/admin
test.mode = ${test.mode}

# Misc Configuration
serial.number.length = 5

# Date/Time Format
date.format = MM/dd/yyyy
date.time.format = MM/dd/yyyy HHmm

# SMTP Configuration
mail.host = mail-host
mail.from = SexualAssaultKitTracking<no-reply@isp.idaho.gov>
# test mailer will send all emails generated by the system to the test destinations(s) configured (see dev.properties)
use.test.mailer = ${use.test.mailer}
test.destinations = ${test.destinations}
errors.email.subject = ${errors.email.subject}
errors.email.from = ${errors.email.from}
errors.email.to = ${errors.email.to}

# Database Configuration
db.type = ${db.type}
db.hibernate.dialect = ${db.hibernate.dialect}
db.hibernate.show.sql = ${db.hibernate.show.sql}
db.hibernate.generate.ddl = ${db.hibernate.generate.ddl}

# LDAP (Active Directory) Configuration
ldap.auth.enabled = false
ldap.url = ldaps://active-directory-server:636
ldap.base = DC=ISP,DC=STATE,DC=ID,DC=US
ldap.user.dn = CN=AppMgr,CN=Users,DC=ISP,DC=STATE,DC=ID,DC=US
ldap.user.password = /9Kzt&^B^v@R$jGk
ldap.auth.user.search.base = OU=ISP Users OU
ldap.auth.user.search.filter = sAMAccountName={0}
ldap.auth.group.search.base = OU=App-Dev OU
ldap.auth.group.search.filter = member={0}